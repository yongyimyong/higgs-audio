build:
  gpu: true
  cuda: "11.8"
  python_version: "3.10"
  system_packages:
    - "ffmpeg"
    - "libsndfile1"
  python_packages:
    - "torch>=2.0.0"
    - "torchaudio>=2.0.0"
    - "transformers>=4.30.0"
    - "accelerate>=0.20.0"
    - "librosa>=0.10.0"
    - "pydub>=0.25.0"
    - "loguru>=0.7.0"
    - "click>=8.0.0"
    - "git+https://github.com/boson-ai/higgs-audio.git"

predict: predict.py::predict
